<div>

<%= form_for [ @article, @comment ], :remote=> true do |f| %>
<% if @comment.article.errors.any? %>

    <div id="error_explanation">
      <h2><%= pluralize(@comment.article.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @comment.article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <%end%>
  <p class="input-form-comment">
    <%= f.text_area :body,:required => true,:class=>'form-control',:placeholder=>'Post Your Comment'%>
  </p>
  <p>
    <%= f.submit 'Comment',:class=>'btn ytp' %>
  </p>
<% end %>
</div>