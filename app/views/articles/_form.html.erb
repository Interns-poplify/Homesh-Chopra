<%= form_for(@article)  do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
  
  <div>
    <%=f.label :title,'Title'%>
    <%=f.text_field :title,:class=>"form-control vv"%><br>
     <%= f.label :tag_list %>
  <%= f.text_field :tag_list,:class=>"form-control vv" %>
  Enter a Comma-separated list of Words to Describe your Content<br>
  <br>
  </div>
  <%=f.label :description,'Description'%>
    <div>
  
    <%=f.text_area :description,:class=>'form-control wysihtml5%'%>
    </div>
<br>
<b>Images<b><br>
  <div >
  <%= f.fields_for :attachments do |photo| %>
   <%= photo.file_field :image%>
   <%end%>
  </div><br>
  <div class="actions ">
    <%= f.submit class: 'btn btn-success'%>
  </div><br>
  </div>

  
<% end %>

   <script>
           
            $('textarea').wysihtml5(


{
   toolbar: {
    "font-styles": true, // Font styling, e.g. h1, h2, etc.
    "emphasis": true, // Italics, bold, etc.
    "lists": true, // (Un)ordered lists, e.g. Bullets, Numbers.
    "html": false, // Button which allows you to edit the generated HTML.
    "link": true, // Button to insert a link.
    "image": false, // Button to insert an image.
    "color": false, // Button to change color of font
    "blockquote": false, // Blockquote
    
  }

}



              );
        
        </script>